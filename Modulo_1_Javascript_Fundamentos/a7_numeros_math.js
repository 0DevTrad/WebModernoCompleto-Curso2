// Calculando a área

const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);

console.log(area);
console.log(typeof Math); //object

Math.PI; // Constante PI 3,1415...
Math.pow; // Calcula a potência (x ^ y)

/*
    Math é um objeto com vários métodos que podem
    ser úteis na hora de realizar operações matemáticas,
    evitando que sejam feitas de formas literais.
*/
